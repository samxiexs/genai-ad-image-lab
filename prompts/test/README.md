# Test Prompt Archive

This folder stores the prompt source files that were originally kept directly under `prompts/`.

Compatibility note:

- The original filenames still exist in `prompts/` as symlinks to this folder.
- Existing `--prompt-version` calls, direct prompt paths, and `aliases/` should continue to work without changes.

Root-level files intentionally left outside this archive:

- `orientation_prompts.json`
- `aliases/`
- `test/`
